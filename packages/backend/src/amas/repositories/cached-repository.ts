/**
 * AMAS 命名空间 - 缓存仓库导出
 *
 * 兼容层：重导出 `src/repositories/cached-repository`。
 */

export {
  CachedStateRepository,
  CachedModelRepository,
  cachedStateRepository,
  cachedModelRepository,
} from '../../repositories/cached-repository';
