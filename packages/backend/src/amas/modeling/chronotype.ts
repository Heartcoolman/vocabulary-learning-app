/**
 * Chronotype Detector (时间节律分析器)
 *
 * 兼容层：历史上部分模块从 `amas/modeling/chronotype` 引入，
 * 实现已集中在 `amas/models/cognitive.ts`，此处提供重导出以保持路径稳定。
 */

export { ChronotypeDetector, type ChronotypeProfile } from '../models/cognitive';
